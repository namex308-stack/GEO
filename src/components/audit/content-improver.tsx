"use client";

/**
 * Pro-only Gemini text improver (canonical path under components/audit).
 * Implementation lives in `@/components/ContentImprover` — re-exported here
 * so Phase 5 path and existing `/tools/content-improver` stay in sync.
 */
export { ContentImprover } from "@/components/ContentImprover";
